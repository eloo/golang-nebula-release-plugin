language: java
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
jdk:
  - oraclejdk8
if: tag IS blank

before_install:
- sed -i 's/git:/https:/g' .git/config

script: "./travis/buildViaTravis.sh"
